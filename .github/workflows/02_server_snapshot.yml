name: 02a_server_snapshot (server read-only snapshot + active_plugins_meta)

on:
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: server-snapshot
  cancel-in-progress: true

jobs:
  snapshot:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    env:
      RUN_ID: ${{ github.run_id }}
      HOST:   ${{ secrets.DEPLOY_HOST }}
      PORT:   ${{ secrets.DEPLOY_PORT }}
      USER:   ${{ secrets.DEPLOY_USER }}
      PASS:   ${{ secrets.DEPLOY_PASS }}
      TARGET: ${{ secrets.DEPLOY_TARGET }}
      WTP_CI: "1"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Sanity secrets
        shell: bash
        run: |
          set -euo pipefail
          for v in HOST PORT USER PASS TARGET; do
            [ -n "${!v:-}" ] || { echo "::error::Missing secret/env $v"; exit 1; }
          done
          echo "::add-mask::$HOST"
          echo "::add-mask::$USER"

      - name: Install tools
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update -y
          sudo apt-get install -y sshpass jq

      - name: Prepare local log dir
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "_ci_logs/${RUN_ID}/server"

      - name: Create remote gather script
        shell: bash
        run: |
          set -euo pipefail
          cat > /tmp/remote_gather.sh <<'SH'
          set -euo pipefail
          cd "$TARGET"

          # sanity
          which php   >/dev/null 2>&1 || { echo "::error::php CLI not available"; exit 1; }
          php -v      >/dev/null 2>&1 || true

          # ensure WP-CLI (local)
          if [ ! -x ./wp ]; then
            curl -sS -L -o wp.phar https://github.com/wp-cli/wp-cli/releases/latest/download/wp-cli.phar
            php wp.phar --info >/dev/null 2>&1
            chmod +x wp.phar && mv wp.phar wp
          fi

          mkdir -p .wtp/state/ci_logs
          {
            uname -a
          } > .wtp/state/ci_logs/uname.txt 2>&1 || true
          {
            php -v
          } > .wtp/state/ci_logs/php_version.txt 2>&1 || true
          {
            pwd
          } > .wtp/state/ci_logs/pwd.txt 2>&1 || true

          # raw dumps (JSON/text) – nic wrażliwego
          php ./wp core version --path="$TARGET"           > .wtp/state/ci_logs/wp_version.txt  2>&1 || true
          php ./wp option get siteurl --path="$TARGET"     > .wtp/state/ci_logs/siteurl.txt     2>&1 || true
          php ./wp theme list --format=json --path="$TARGET"    > .wtp/state/ci_logs/themes.json     2>&1 || echo "[]" > .wtp/state/ci_logs/themes.json
          php ./wp plugin list --format=json --path="$TARGET"   > .wtp/state/ci_logs/plugins.json    2>&1 || echo "[]" > .wtp/state/ci_logs/plugins.json
          php ./wp option get active_plugins --format=json --path="$TARGET" > .wtp/state/ci_logs/active_plugins.json 2>&1 || echo "[]" > .wtp/state/ci_logs/active_plugins.json
          # aktywny motyw (stylesheet)
          php ./wp option get stylesheet --path="$TARGET"   > .wtp/state/ci_logs/active_theme.txt 2>&1 || true
          SH

      - name: Run remote gather
        shell: bash
        run: |
          set -euo pipefail
          sshpass -p "$PASS" scp -P "$PORT" -o StrictHostKeyChecking=no /tmp/remote_gather.sh "$USER@$HOST:/tmp/remote_gather.sh"
          sshpass -p "$PASS" ssh -p "$PORT" -o StrictHostKeyChecking=no "$USER@$HOST" "export TARGET='$TARGET' && bash /tmp/remote_gather.sh && rm -f /tmp/remote_gather.sh"

      - name: Pull raw dumps back
        shell: bash
        run: |
          set -euo pipefail
          sshpass -p "$PASS" scp -P "$PORT" -o StrictHostKeyChecking=no -r "$USER@$HOST:$TARGET/.wtp/state/ci_logs/" "_ci_logs/${RUN_ID}/server/"

      - name: Build snapshot.json + active_plugins_meta.json (locally)
        shell: bash
        run: |
          set -euo pipefail
          shopt -s nullglob  # <— kluczowe: globy bez dopasowań dają pustą listę
          ROOT="_ci_logs/${RUN_ID}/server"
          mkdir -p "$ROOT" ".wtp/state/ro/public/${RUN_ID}/server" ".wtp/state/ro/public/latest/server"

          # Zbierz slugi z active_plugins.json (tablica lub pustka)
          if jq -e 'type=="array"' "$ROOT/active_plugins.json" >/dev/null 2>&1; then
            mapfile -t SLUGS < <(jq -r '.[] | strings' "$ROOT/active_plugins.json" | sed 's/\.php$//' | sed 's#.*/##' | sort -u)
          else
            SLUGS=()
          fi

          mkdir -p "$ROOT/active_meta"
          if [ "${#SLUGS[@]}" -gt 0 ]; then
            for sl in "${SLUGS[@]}"; do
              sshpass -p "$PASS" ssh -p "$PORT" -o StrictHostKeyChecking=no "$USER@$HOST" \
                "cd '$TARGET' && php ./wp plugin get '${sl}' --fields=name,version,author,plugin_uri,requires,requires_php,update,title,description,slug,status --format=json --path='$TARGET' 2>/dev/null || echo '{}'" \
                > "$ROOT/active_meta/${sl}.json" || echo "{}" > "$ROOT/active_meta/${sl}.json"
            done
          fi

          # Agregacja meta aktywnych wtyczek — bezpieczna gdy brak plików
          META_FILES=("$ROOT/active_meta/"*.json)
          if [ ${#META_FILES[@]} -gt 0 ]; then
            jq -s 'map(select(type=="object"))' "${META_FILES[@]}" > "$ROOT/active_plugins_meta.json"
          else
            echo "[]" > "$ROOT/active_plugins_meta.json"
          fi

          # themes.json → active_theme (fallback: option stylesheet)
          ACTIVE_THEME="$(cat "$ROOT/active_theme.txt" 2>/dev/null || true)"
          if [ -z "$ACTIVE_THEME" ] || [ "$ACTIVE_THEME" = "null" ]; then
            ACTIVE_THEME="$(jq -r '.[] | select(.status=="active") | .stylesheet' "$ROOT/themes.json" 2>/dev/null | head -n1)"
          fi

          SITEURL="$(cat "$ROOT/siteurl.txt" 2>/dev/null || true)"
          WP_VERSION="$(cat "$ROOT/wp_version.txt" 2>/dev/null || true)"
          PHPV="$(head -n1 "$ROOT/php_version.txt" 2>/dev/null || true)"
          UNAME="$(cat "$ROOT/uname.txt" 2>/dev/null || true)"
          PWD="$(cat "$ROOT/pwd.txt" 2>/dev/null || true)"

          jq -n \
            --arg run_id        "${RUN_ID}" \
            --arg siteurl       "${SITEURL}" \
            --arg wp_version    "${WP_VERSION}" \
            --arg php_version   "${PHPV}" \
            --arg uname         "${UNAME}" \
            --arg pwd           "${PWD}" \
            --arg active_theme  "${ACTIVE_THEME}" \
            --slurpfile themes  "$ROOT/themes.json" \
            --slurpfile plugins "$ROOT/plugins.json" \
            --slurpfile act     "$ROOT/active_plugins.json" \
            --slurpfile apm     "$ROOT/active_plugins_meta.json" \
            '{
              run_id: $run_id,
              server: { uname: $uname, php_version: $php_version },
              wp: { version: $wp_version, siteurl: $siteurl, path: $pwd, active_theme: $active_theme },
              themes: ( ($themes|first)//[] ),
              plugins: ( ($plugins|first)//[] ),
              active_plugins: ( ($act|first)//[] ),
              active_plugins_meta: ( ($apm|first)//[] )
            }' > ".wtp/state/ro/public/${RUN_ID}/server/snapshot.json"

          {
            echo "# SERVER SNAPSHOT"
            echo "- Run ID: ${RUN_ID}"
            echo "- Site URL: ${SITEURL}"
            echo "- WP: ${WP_VERSION}"
            echo "- PHP: ${PHPV}"
            echo "- Active theme: ${ACTIVE_THEME}"
            echo ""
            echo "## Active plugins (${#SLUGS[@]})"
            for sl in "${SLUGS[@]}"; do echo "- ${sl}"; done
          } > ".wtp/state/ro/public/${RUN_ID}/server/SERVER_SNAPSHOT.md"

          cp -f ".wtp/state/ro/public/${RUN_ID}/server/snapshot.json" ".wtp/state/ro/public/latest/server/snapshot.json"
          cp -f ".wtp/state/ro/public/${RUN_ID}/server/SERVER_SNAPSHOT.md" ".wtp/state/ro/public/latest/server/SERVER_SNAPSHOT.md"
          cp -f "$ROOT/active_plugins_meta.json" ".wtp/state/ro/public/${RUN_ID}/server/active_plugins_meta.json"
          cp -f "$ROOT/active_plugins_meta.json" ".wtp/state/ro/public/latest/server/active_plugins_meta.json"
      - name: Commit & push (rebase-safe)
        shell: bash
        run: |
          set -euo pipefail
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --staged --quiet; then
            echo "::notice::Nothing to commit."
            exit 0
          fi
          git commit -m "server snapshot ${RUN_ID}"
          BRANCH="${GITHUB_REF_NAME:-main}"
          git branch --show-current >/dev/null 2>&1 || git checkout -B "$BRANCH"
          for i in 1 2 3; do
            git fetch origin "$BRANCH"
            git pull --rebase origin "$BRANCH" || { git rebase --continue || true; }
            if git push origin "HEAD:$BRANCH"; then
              echo "::notice::Pushed (attempt $i)"
              exit 0
            fi
            sleep 2
          done
          echo "::error::Push failed after 3 attempts"
          exit 1
