diff --git a/mu-plugins/wtp-ro-exporter.php b/mu-plugins/wtp-ro-exporter.php
deleted file mode 100644
index 1111111..0000000
--- a/mu-plugins/wtp-ro-exporter.php
+++ /dev/null
@@ -1,210 +0,0 @@
-<?php
-/**
- * Plugin Name: WTP Read-Only Exporter (open)
- * Description: Stabilne endpointy do snapshotu (lista plików i pobieranie pojedynczych plików) z katalogu uploads/wtp-ro/public/{site_key}/
- * Version: 1.1.0
- */
-if (!defined('ABSPATH')) exit;
-
-define('WTP_RO_EXPORTER_NS', 'wtp-ro-open/v1');
-$wtp_ro_default_site_key = '5Depft8Y9LU0t6Sv';
-
-function wtp_ro_exporter_base_dir() {
-    $dir = WP_CONTENT_DIR . '/uploads/wtp-ro/public/';
-    return rtrim($dir, '/').'/';
-}
-
-function wtp_ro_sanitize_site_key($raw, $fallback) {
-    $k = is_string($raw) && $raw !== '' ? $raw : $fallback;
-    $k = preg_replace('/[^A-Za-z0-9]/', '', $k);
-    if ($k === '') $k = $fallback;
-    return $k;
-}
-
-add_filter('rest_pre_serve_request', function ($served, $result, $request, $server) {
-    $route = $request->get_route();
-    if (strpos($route, '/'.WTP_RO_EXPORTER_NS.'/') !== false) {
-        @header('Access-Control-Allow-Origin: *');
-        @header('Access-Control-Allow-Methods: GET, OPTIONS');
-        @header('Access-Control-Allow-Headers: *');
-        @header('Cache-Control: no-cache, no-store, must-revalidate');
-        @header('Pragma: no-cache');
-        @header('Expires: 0');
-    }
-    return $served;
-}, 10, 4);
-
-add_action('rest_api_init', function () use ($wtp_ro_default_site_key) {
-    register_rest_route(WTP_RO_EXPORTER_NS, '/ls', [
-        'methods'  => 'GET',
-        'permission_callback' => '__return_true',
-        'callback' => function (\WP_REST_Request $req) use ($wtp_ro_default_site_key) {
-            $site_key = wtp_ro_sanitize_site_key($req->get_param('site_key'), $wtp_ro_default_site_key);
-            $base     = wtp_ro_exporter_base_dir();
-            $root     = realpath($base);
-            $dir      = realpath($base.$site_key);
-            if (!$root || !$dir || strpos($dir, $root) !== 0 || !is_dir($dir)) {
-                return new \WP_REST_Response([
-                    'version'      => '1.5.0',
-                    'generated_at' => time(),
-                    'url'          => content_url("/uploads/wtp-ro/public/{$site_key}"),
-                    'files'        => [],
-                    'manifest'     => 'manifest.json',
-                    'options'      => 'options.json',
-                    'selftest'     => 'selftest.json',
-                ], 200);
-            }
-            $list = [];
-            $dh = opendir($dir);
-            if ($dh) {
-                while (($f = readdir($dh)) !== false) {
-                    if ($f === '.' || $f === '..') continue;
-                    $real = realpath($dir.'/'.$f);
-                    if (!$real || strpos($real, $dir) !== 0) continue;
-                    if (is_file($real)) {
-                        $list[] = $f;
-                    }
-                }
-                closedir($dh);
-            }
-            sort($list, SORT_STRING);
-            return new \WP_REST_Response([
-                'version'      => '1.5.0',
-                'generated_at' => time(),
-                'url'          => content_url("/uploads/wtp-ro/public/{$site_key}"),
-                'files'        => $list,
-                'manifest'     => 'manifest.json',
-                'options'      => 'options.json',
-                'selftest'     => 'selftest.json',
-            ], 200);
-        }
-    ]);
-
-    register_rest_route(WTP_RO_EXPORTER_NS, '/get', [
-        'methods'  => 'GET',
-        'permission_callback' => '__return_true',
-        'callback' => function (\WP_REST_Request $req) use ($wtp_ro_default_site_key) {
-            $site_key = wtp_ro_sanitize_site_key($req->get_param('site_key'), $wtp_ro_default_site_key);
-            $file     = $req->get_param('file');
-            if (!is_string($file) || $file === '') {
-                return new \WP_Error('bad_file', 'Invalid file name', ['status' => 400]);
-            }
-            if (!preg_match('/^[A-Za-z0-9._-]+$/', $file)) {
-                return new \WP_Error('bad_file', 'Invalid file name', ['status' => 400]);
-            }
-            $base = wtp_ro_exporter_base_dir();
-            $root = realpath($base);
-            $dir  = realpath($base.$site_key);
-            if (!$root || !$dir || strpos($dir, $root) !== 0) {
-                return new \WP_Error('not_found', 'Base path not found', ['status' => 404]);
-            }
-            $path = realpath($dir.'/'.$file);
-            if (!$path || strpos($path, $dir) !== 0 || !is_file($path)) {
-                return new \WP_Error('not_found', 'File not found', ['status' => 404]);
-            }
-            $data = @file_get_contents($path);
-            if ($data === false) {
-                return new \WP_Error('read_error', 'Could not read file', ['status' => 500]);
-            }
-            $ext = strtolower(pathinfo($file, PATHINFO_EXTENSION));
-            $ct  = 'text/plain; charset=UTF-8';
-            if ($ext === 'json') $ct = 'application/json; charset=UTF-8';
-            if ($ext === 'txt' ) $ct = 'text/plain; charset=UTF-8';
-            return new \WP_REST_Response($data, 200, ['Content-Type' => $ct]);
-        }
-    ]);
-
-    register_rest_route(WTP_RO_EXPORTER_NS, '/health', [
-        'methods'  => 'GET',
-        'permission_callback' => '__return_true',
-        'callback' => function (\WP_REST_Request $req) use ($wtp_ro_default_site_key) {
-            $site_key = wtp_ro_sanitize_site_key($req->get_param('site_key'), $wtp_ro_default_site_key);
-            $base = wtp_ro_exporter_base_dir();
-            $ok   = is_dir($base.$site_key);
-            return new \WP_REST_Response([
-                'ok'        => $ok ? true : false,
-                'site_key'  => $site_key,
-                'base'      => $base,
-                'ts'        => time(),
-            ], 200);
-        }
-    ]);
-});
-
-if (!function_exists('wtp_ro_exporter_loaded_flag')) {
-    function wtp_ro_exporter_loaded_flag() { return true; }
-}
diff --git a/mu-plugins.disabled/wtp-ro-exporter.php b/mu-plugins.disabled/wtp-ro-exporter.php
new file mode 100644
index 0000000..2222222
--- /dev/null
+++ b/mu-plugins.disabled/wtp-ro-exporter.php
@@ -0,0 +1,240 @@
+<?php
+/**
+ * Plugin Name: WTP Read-Only Exporter (open) — SAFE & DISABLED
+ * Description: Poprawiony i bezpieczny eksporter (REST). W tej chwili WYŁĄCZONY operacyjnie, bo plik jest w mu-plugins.disabled/.
+ * Version: 1.1.1
+ */
+if (!defined('ABSPATH')) { /* jeżeli coś go wczyta poza WP – wyjdź bez fatalu */ return; }
+
+// Bezpieczne gardy – jeżeli środowisko nie jest gotowe, nic nie rejestruj:
+if (!function_exists('add_action') || !function_exists('register_rest_route')) {
+    return;
+}
+if (!defined('WP_CONTENT_DIR')) {
+    // nietypowe/lightspeed-early: ochronnie wyjdź
+    return;
+}
+
+// Stałe i konfiguracja
+define('WTP_RO_EXPORTER_NS', 'wtp-ro-open/v1');
+// Domyślny site_key – zostawiamy Twój obecny
+$wtp_ro_default_site_key = '5Depft8Y9LU0t6Sv';
+
+// Katalog bazowy snapshotów
+function wtp_ro_exporter_base_dir_safe() {
+    $dir = rtrim(WP_CONTENT_DIR, '/').'/uploads/wtp-ro/public/';
+    return $dir;
+}
+
+// Walidacja site_key (A-Za-z0-9)
+function wtp_ro_sanitize_site_key_safe($raw, $fallback) {
+    $k = (is_string($raw) && $raw !== '') ? $raw : $fallback;
+    $k = preg_replace('/[^A-Za-z0-9]/', '', $k);
+    if ($k === '') $k = $fallback;
+    return $k;
+}
+
+// Nagłówki CORS/cache tylko dla naszej przestrzeni
+add_filter('rest_pre_serve_request', function ($served, $result, $request, $server) {
+    try {
+        $route = method_exists($request, 'get_route') ? $request->get_route() : '';
+        if (is_string($route) && strpos($route, '/'.WTP_RO_EXPORTER_NS.'/') !== false) {
+            @header('Access-Control-Allow-Origin: *');
+            @header('Access-Control-Allow-Methods: GET, OPTIONS');
+            @header('Access-Control-Allow-Headers: *');
+            @header('Cache-Control: no-cache, no-store, must-revalidate');
+            @header('Pragma: no-cache');
+            @header('Expires: 0');
+        }
+    } catch (\Throwable $e) {
+        // cicho – żadnych fatal errors
+    }
+    return $served;
+}, 10, 4);
+
+// Rejestracja endpointów
+add_action('rest_api_init', function () use ($wtp_ro_default_site_key) {
+    try {
+        register_rest_route(WTP_RO_EXPORTER_NS, '/ls', [
+            'methods'  => 'GET',
+            'permission_callback' => '__return_true',
+            'callback' => function (\WP_REST_Request $req) use ($wtp_ro_default_site_key) {
+                $site_key = wtp_ro_sanitize_site_key_safe($req->get_param('site_key'), $wtp_ro_default_site_key);
+                $base     = wtp_ro_exporter_base_dir_safe();
+                $root     = @realpath($base);
+                $dir      = @realpath($base.$site_key);
+
+                if (!$root || !$dir || strpos($dir, $root) !== 0 || !is_dir($dir)) {
+                    return new \WP_REST_Response([
+                        'version'      => '1.5.0',
+                        'generated_at' => time(),
+                        'url'          => content_url("/uploads/wtp-ro/public/{$site_key}"),
+                        'files'        => [],
+                        'manifest'     => 'manifest.json',
+                        'options'      => 'options.json',
+                        'selftest'     => 'selftest.json',
+                    ], 200);
+                }
+
+                $list = [];
+                $dh = @opendir($dir);
+                if ($dh) {
+                    while (($f = readdir($dh)) !== false) {
+                        if ($f === '.' || $f === '..') continue;
+                        $real = @realpath($dir.'/'.$f);
+                        if (!$real || strpos($real, $dir) !== 0) continue;
+                        if (is_file($real)) $list[] = $f;
+                    }
+                    @closedir($dh);
+                }
+
+                sort($list, SORT_STRING);
+                return new \WP_REST_Response([
+                    'version'      => '1.5.0',
+                    'generated_at' => time(),
+                    'url'          => content_url("/uploads/wtp-ro/public/{$site_key}"),
+                    'files'        => $list,
+                    'manifest'     => 'manifest.json',
+                    'options'      => 'options.json',
+                    'selftest'     => 'selftest.json',
+                ], 200);
+            }
+        ]);
+
+        register_rest_route(WTP_RO_EXPORTER_NS, '/get', [
+            'methods'  => 'GET',
+            'permission_callback' => '__return_true',
+            'callback' => function (\WP_REST_Request $req) use ($wtp_ro_default_site_key) {
+                $site_key = wtp_ro_sanitize_site_key_safe($req->get_param('site_key'), $wtp_ro_default_site_key);
+                $file     = $req->get_param('file');
+                if (!is_string($file) || $file === '') {
+                    return new \WP_Error('bad_file', 'Invalid file name', ['status' => 400]);
+                }
+                if (!preg_match('/^[A-Za-z0-9._-]+$/', $file)) {
+                    return new \WP_Error('bad_file', 'Invalid file name', ['status' => 400]);
+                }
+
+                $base = wtp_ro_exporter_base_dir_safe();
+                $root = @realpath($base);
+                $dir  = @realpath($base.$site_key);
+                if (!$root || !$dir || strpos($dir, $root) !== 0) {
+                    return new \WP_Error('not_found', 'Base path not found', ['status' => 404]);
+                }
+                $path = @realpath($dir.'/'.$file);
+                if (!$path || strpos($path, $dir) !== 0 || !is_file($path)) {
+                    return new \WP_Error('not_found', 'File not found', ['status' => 404]);
+                }
+                $data = @file_get_contents($path);
+                if ($data === false) {
+                    return new \WP_Error('read_error', 'Could not read file', ['status' => 500]);
+                }
+                $ext = strtolower(pathinfo($file, PATHINFO_EXTENSION));
+                $ct  = ($ext === 'json') ? 'application/json; charset=UTF-8' : 'text/plain; charset=UTF-8';
+                return new \WP_REST_Response($data, 200, ['Content-Type' => $ct]);
+            }
+        ]);
+
+        register_rest_route(WTP_RO_EXPORTER_NS, '/health', [
+            'methods'  => 'GET',
+            'permission_callback' => '__return_true',
+            'callback' => function (\WP_REST_Request $req) use ($wtp_ro_default_site_key) {
+                $site_key = wtp_ro_sanitize_site_key_safe($req->get_param('site_key'), $wtp_ro_default_site_key);
+                $base = wtp_ro_exporter_base_dir_safe();
+                $ok   = is_dir($base.$site_key);
+                return new \WP_REST_Response([
+                    'ok'        => $ok ? true : false,
+                    'site_key'  => $site_key,
+                    'base'      => $base,
+                    'ts'        => time(),
+                ], 200);
+            }
+        ]);
+    } catch (\Throwable $e) {
+        // cicho – żadnych fatal errors
+    }
+});
+
+if (!function_exists('wtp_ro_exporter_loaded_flag')) {
+    function wtp_ro_exporter_loaded_flag() { return true; }
+}
diff --git a/mu-plugins.disabled/README.md b/mu-plugins.disabled/README.md
new file mode 100644
index 0000000..3333333
--- /dev/null
+++ b/mu-plugins.disabled/README.md
@@ -0,0 +1,9 @@
+# MU Plugins (disabled)
+
+Pliki w tym katalogu **nie są ładowane** przez WordPressa.
+To „parking” dla rzeczy, które chcemy mieć w repo, ale nie aktywować.
+
+aby włączyć — przenieś plik z `mu-plugins.disabled/` do `mu-plugins/`
+i uruchom workflow **Deploy Site** (albo zrób to ręcznie).
+
+WTP.
