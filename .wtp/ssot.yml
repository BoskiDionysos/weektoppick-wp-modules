deploy:
  host_secret: DEPLOY_HOST
  port_secret: DEPLOY_PORT
  user_secret: DEPLOY_USER
  pass_secret: DEPLOY_PASS
  target_secret: DEPLOY_TARGET
site:
  base_url: https://weektoppick.com
plugins:
  install:
    - classic-editor
    - litespeed-cache
    - wordfence
    - cookie-law-info
    - seo-by-rank-math
    - pretty-links
ci:
  locks_bypass: "env:WTP_CI"
