<?php /**  * WTP Core Theme — SAFE functions.php  * Usunięto HTML/rendering, tylko bootstrap hooków  */  if (!defined('ABSPATH')) {     exit; }  // Podstawowe wsparcie add_action('after_setup_theme', function () {     add_theme_support('title-tag');     add_theme_support('post-thumbnails');     add_theme_support('html5', ['search-form','gallery','caption','style','script']); }, 0);  // Kolejne hooki (puste, bezpieczne) add_action('init', function () {     // Rejestracje CPT/tax wrócą po diagnozie }, 1);  add_action('wp_enqueue_scripts', function () {     // CSS/JS w następnym kroku }, 10);  // Placeholder pod shortcody/widżety // add_shortcode('wtp_top3', 'wtp_render_top3');  // Tymczasowo render włączymy przez oddzielny plik template-part